# MA Project Master Configuration
# This file controls the entire pipeline execution

# Teams per batch 
TEAMS_PER_BATCH=10

# Number of batches
NUM_BATCHES=20

# Simulations per batch (round-robin matches per batch)  
SIMULATIONS_PER_BATCH=45

# Total game component number
TOTAL_GAMECOMPONENTNUMBER=50